import { ImageCategoryPipe } from './image-category.pipe';

describe('ImageCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
